# Fix Auth "Fail to Fetch" - Backend Mock Setup

## Steps (0/7 complete)

- [x] 1. Update package.json (add json-server dev dep + npm script)
- [x] 2. Create db.json (mock users data)  
- [x] 3. Create api-routes.json (custom /api/auth routes)
- [x] 4. Create auth.js (middleware for login/signup logic)
- [x] 5. Install dependencies (`npm install`)
- [x] 6. Test API server (`npm run dev:api`)
- [x] 7. Test full app (run frontend + API, try login/signup)

**Notes:** 
- Backend runs on http://localhost:8080/api
- After all steps, auth should work without fetch errors.
- Run `npm run dev:api` in one terminal, `npm start` in another.

## Progress Tracking
Update checklist as steps complete.

